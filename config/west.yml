manifest:
  remotes:
    - name: zmkfirmware
      url-base: https://github.com/zmkfirmware
  projects:
    - name: zmk
      remote: zmkfirmware
      # revision: v0.3 # avoid compatibility break
      # TODO: Try build main eventually
      revision: main
      import: app/west.yml
  self:
    path: config
